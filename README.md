# **TestFrontend**

Aby zainstalować ten przykład, uruchom następujące polecenia:
```bash
cd backend

mvn clean install

docker-compose up -d
```
Dostęp po adresem: 

[http://localhost:3000](http://localhost:3000)


